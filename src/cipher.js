const cipher = {
  encode: function(offset, mensagem) {
    // algoritmo da cifra de cesar para codificar mensagem
    const msgCifrada = mensagem // por enquanto retorna msg original
    return msgCifrada
  },
  decode: function(offset, mensagem) {
    // algoritmo da cifra de cesar para decodificar mensagem
    const msgDecifrada = mensagem // por enquanto retorna msg original
    return msgDecifrada
  },
};

export default cipher;
